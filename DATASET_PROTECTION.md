# Protected Research Data

`D:\document\work\binance_trade_assistant\research_dataset` is a protected local research asset.

Rules for every future cleanup:

1. Never delete or recursively remove `research_dataset`.
2. Never include it in cleanup patterns for old builds, releases, reports, or executable versions.
3. Preserve `dataset_manifest.json` and all partitioned `.jsonl` files.
4. Ask for explicit approval before any retention, archive, move, or deletion operation involving this directory.
